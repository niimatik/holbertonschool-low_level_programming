#include "main.h"
#include <stdio.h>



void print_diagonal(int n)
{
	int num, space;

	if (n <= 0)
	{
		_putchar('\n');
	}
	else

	for (num = 0; num < n; num++)
	{
		for (space = 0; space < num; space++)
		{
			_putchar(' ');
		}
		_putchar('\\');
		_putchar('\n');
	}
}
