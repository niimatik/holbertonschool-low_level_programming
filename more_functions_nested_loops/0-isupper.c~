#include "main.h"
#include <stdio.h>

/**
 * _islower - Checks if a character is a uppercase letter.
 * @c: The character to check.
 *
 * Return: 1 if c is a uppercase letter, 0 otherwise.
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
