#include "main.h"
#include <stdio.h>

/**
 *_islower display 1 if lowercase 0 otherwise
 *
 *Return 0: Always (success)
 */

int _islower(int c)
{
	if (islower(c) !=0)
	{
		_putchar('0');
	}
	_putchar('\n');
	return (0);
}
