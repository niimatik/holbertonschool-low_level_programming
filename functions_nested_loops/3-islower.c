#include "main.h"
#include <stdio.h>

/**
 *_islower display 1 if lowercase 0 otherwise
 *
 *Return 0: Always (success)
 */

int _islower(int c)
{
	if (_islower(r) !=0)
	{
		_putchar('0');
	}
	else
	{
		_putchar('1');
	}
	_putchar('\n');
	return (0);
}
