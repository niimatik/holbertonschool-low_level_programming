#include "main.h"
#include <stdio.h>

/**
 * _islower - Checks if a character is a upper or lowercase lettre.
 * @c: The character to check.
 *
 * Return: 1 if c is a upper or lowercase letter, 0 otherwise.
 */

int _isalpha(int c)
{
	if (c >= 'a' && c <= 'Z')
		return (1);
	else
		return (0);
}
